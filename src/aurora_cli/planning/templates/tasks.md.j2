# Task Checklist: {{ plan_name | title }}

**Plan ID**: `{{ plan_id }}`
**Created**: {{ created_at }}
**Last Updated**: {{ updated_at | default(created_at) }}

---

## Progress Summary

- **Total Tasks**: {{ total_tasks | default(0) }}
- **Completed**: {{ completed_tasks | default(0) }}
- **In Progress**: {{ in_progress_tasks | default(0) }}
- **Pending**: {{ pending_tasks | default(0) }}
- **Progress**: {{ progress_percentage | default(0) }}%

---

## Tasks

{% for subgoal in subgoals %}
### {{ subgoal.id | upper }}: {{ subgoal.title }}

**Agent**: {{ subgoal.recommended_agent }}
**Status**: {{ subgoal.status }}
{% if subgoal.dependencies %}**Depends on**: {{ subgoal.dependencies | join(', ') }}{% endif %}

{% if file_resolutions and subgoal.id in file_resolutions %}
**Relevant Files**:
{% for resolution in file_resolutions[subgoal.id] %}
{% if resolution.confidence >= 0.8 %}
- `{{ resolution.path }}` lines {{ resolution.line_start }}-{{ resolution.line_end }}
{% elif resolution.confidence >= 0.6 %}
- `{{ resolution.path }}` lines {{ resolution.line_start }}-{{ resolution.line_end }} (suggested)
{% else %}
- `{{ resolution.path }}` lines {{ resolution.line_start }}-{{ resolution.line_end }} (low confidence)
{% endif %}
{% endfor %}
{% elif file_resolutions is not defined or (file_resolutions and subgoal.id not in file_resolutions) %}
**Relevant Files**: TBD - run `aur mem index .` for code-aware file resolution
{% endif %}

{% for task in subgoal.get('tasks', []) %}
- [ ] {{ task }}
{% else %}
- [ ] Review requirements for {{ subgoal.title }}
- [ ] Design implementation approach
- [ ] Implement core functionality
- [ ] Write tests
- [ ] Review and refactor
- [ ] Update documentation
{% endfor %}

{% endfor %}

---

## Validation Checklist

Before marking this plan as complete, ensure:

- [ ] All functional requirements implemented (see prd.md)
- [ ] All tests passing
- [ ] Code reviewed
- [ ] Documentation updated
- [ ] No critical bugs or blockers
- [ ] Success criteria met

---

## Notes

- Mark tasks complete with `[x]` as you finish them
- Update progress summary regularly
- Add new tasks as they are discovered
- Track blockers in comments or separate issues

---

*Generated by Aurora Planning System v0.1.0*
